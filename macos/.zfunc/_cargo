#compdef cargo
# Source: Wrapper that delegates to rustc sysroot
# Regenerate: N/A (auto-delegates to $(rustc --print sysroot)/share/zsh/site-functions/_cargo)

if command -v rustc >/dev/null 2>&1; then
	source "$(rustc --print sysroot)"/share/zsh/site-functions/_cargo
fi
